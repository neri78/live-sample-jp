{
  "name": "live-sample-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/live-player-sdk": "^1.0.1",
    "@twilio/runtime-handler": "1.2.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.4.0"
  },
  "engines": {
    "node": "14"
  }
}
